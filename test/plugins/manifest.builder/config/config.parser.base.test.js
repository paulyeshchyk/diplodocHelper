const config_parser_base = require('../../../../src/plugins/manifest.builder/config/config.parser.base');

describe('config.parser.base', () => {
    test('ConfigParserBase should be defined', () => {
        expect(config_parser_base.ConfigParserBase).toBeDefined();
    });
    test.todo('ConfigParserBase should work correctly');
});
