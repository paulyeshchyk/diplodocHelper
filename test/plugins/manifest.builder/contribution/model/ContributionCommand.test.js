const ContributionCommand = require('../../../../../src/plugins/manifest.builder/contribution/model/ContributionCommand');

describe('ContributionCommand', () => {
    test('ContributionCommand should be defined', () => {
        expect(ContributionCommand).toBeDefined();
    });
    test.todo('ContributionCommand should work correctly');
});
