const ContributionSubmenu = require('../../../../../src/plugins/manifest.builder/contribution/model/ContributionSubmenu');

describe('ContributionSubmenu', () => {
    test('ContributionSubmenu should be defined', () => {
        expect(ContributionSubmenu).toBeDefined();
    });
    test.todo('ContributionSubmenu should work correctly');
});
