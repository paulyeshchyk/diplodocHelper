const ContributionMenuItem = require('../../../../../src/plugins/manifest.builder/contribution/model/ContributionMenuItem');

describe('ContributionMenuItem', () => {
    test('ContributionMenuItem should be defined', () => {
        expect(ContributionMenuItem).toBeDefined();
    });
    test.todo('ContributionMenuItem should work correctly');
});
