const ContributionMenu = require('../../../../../src/plugins/manifest.builder/contribution/model/ContributionMenu');

describe('ContributionMenu', () => {
    test('ContributionMenu should be defined', () => {
        expect(ContributionMenu).toBeDefined();
    });
    test.todo('ContributionMenu should work correctly');
});
