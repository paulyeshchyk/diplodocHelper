const index = require('../../../src/plugins/utils/index');

describe('index', () => {
    test('isFileProtocol should be defined', () => {
        expect(index.isFileProtocol).toBeDefined();
    });
    test.todo('isFileProtocol should work correctly');
    test('buildBreadcrumbHref should be defined', () => {
        expect(index.buildBreadcrumbHref).toBeDefined();
    });
    test.todo('buildBreadcrumbHref should work correctly');
    test('getImageFromClipboard should be defined', () => {
        expect(index.getImageFromClipboard).toBeDefined();
    });
    test.todo('getImageFromClipboard should work correctly');
    test('createSectionFolder should be defined', () => {
        expect(index.createSectionFolder).toBeDefined();
    });
    test.todo('createSectionFolder should work correctly');
    test('DiplodocSectionRefresh should be defined', () => {
        expect(index.DiplodocSectionRefresh).toBeDefined();
    });
    test.todo('DiplodocSectionRefresh should work correctly');
    test('DiplodocSectionPatch should be defined', () => {
        expect(index.DiplodocSectionPatch).toBeDefined();
    });
    test.todo('DiplodocSectionPatch should work correctly');
    test('getSectionMetadata should be defined', () => {
        expect(index.getSectionMetadata).toBeDefined();
    });
    test.todo('getSectionMetadata should work correctly');
    test('composeFullTitle should be defined', () => {
        expect(index.composeFullTitle).toBeDefined();
    });
    test.todo('composeFullTitle should work correctly');
    test('composeFolderName should be defined', () => {
        expect(index.composeFolderName).toBeDefined();
    });
    test.todo('composeFolderName should work correctly');
    test('isIndexedSectionType should be defined', () => {
        expect(index.isIndexedSectionType).toBeDefined();
    });
    test.todo('isIndexedSectionType should work correctly');
    test('parse should be defined', () => {
        expect(index.parse).toBeDefined();
    });
    test.todo('parse should work correctly');
    test('stringify should be defined', () => {
        expect(index.stringify).toBeDefined();
    });
    test.todo('stringify should work correctly');
    test('get should be defined', () => {
        expect(index.get).toBeDefined();
    });
    test.todo('get should work correctly');
    test('update should be defined', () => {
        expect(index.update).toBeDefined();
    });
    test.todo('update should work correctly');
    test('remove should be defined', () => {
        expect(index.remove).toBeDefined();
    });
    test.todo('remove should work correctly');
    test('getTitleFromMetadata should be defined', () => {
        expect(index.getTitleFromMetadata).toBeDefined();
    });
    test.todo('getTitleFromMetadata should work correctly');
    test('isHtmlFile should be defined', () => {
        expect(index.isHtmlFile).toBeDefined();
    });
    test.todo('isHtmlFile should work correctly');
    test('isRootIndex should be defined', () => {
        expect(index.isRootIndex).toBeDefined();
    });
    test.todo('isRootIndex should work correctly');
    test('IndexMdEntryReadIndex should be defined', () => {
        expect(index.IndexMdEntryReadIndex).toBeDefined();
    });
    test.todo('IndexMdEntryReadIndex should work correctly');
    test('IndexMdEntryReadTitle should be defined', () => {
        expect(index.IndexMdEntryReadTitle).toBeDefined();
    });
    test.todo('IndexMdEntryReadTitle should work correctly');
    test('IndexMdEntryReadSectionType should be defined', () => {
        expect(index.IndexMdEntryReadSectionType).toBeDefined();
    });
    test.todo('IndexMdEntryReadSectionType should work correctly');
    test('IndexMdUpsert should be defined', () => {
        expect(index.IndexMdUpsert).toBeDefined();
    });
    test.todo('IndexMdUpsert should work correctly');
    test('IndexMdFileRead should be defined', () => {
        expect(index.IndexMdFileRead).toBeDefined();
    });
    test.todo('IndexMdFileRead should work correctly');
    test('IndexMdFilePatch should be defined', () => {
        expect(index.IndexMdFilePatch).toBeDefined();
    });
    test.todo('IndexMdFilePatch should work correctly');
    test('ExtractFigures should be defined', () => {
        expect(index.ExtractFigures).toBeDefined();
    });
    test.todo('ExtractFigures should work correctly');
    test('ExtractMdLinks should be defined', () => {
        expect(index.ExtractMdLinks).toBeDefined();
    });
    test.todo('ExtractMdLinks should work correctly');
    test('buildFigure should be defined', () => {
        expect(index.buildFigure).toBeDefined();
    });
    test.todo('buildFigure should work correctly');
    test('buildFigureId should be defined', () => {
        expect(index.buildFigureId).toBeDefined();
    });
    test.todo('buildFigureId should work correctly');
    test('buildImageLink should be defined', () => {
        expect(index.buildImageLink).toBeDefined();
    });
    test.todo('buildImageLink should work correctly');
    test('parseMarkdownLinks should be defined', () => {
        expect(index.parseMarkdownLinks).toBeDefined();
    });
    test.todo('parseMarkdownLinks should work correctly');
    test('splitPathQueryHash should be defined', () => {
        expect(index.splitPathQueryHash).toBeDefined();
    });
    test.todo('splitPathQueryHash should work correctly');
    test('isInsideRenamedFolder should be defined', () => {
        expect(index.isInsideRenamedFolder).toBeDefined();
    });
    test.todo('isInsideRenamedFolder should work correctly');
    test('getNewAbsoluteTarget should be defined', () => {
        expect(index.getNewAbsoluteTarget).toBeDefined();
    });
    test.todo('getNewAbsoluteTarget should work correctly');
    test('buildMarkdownLink should be defined', () => {
        expect(index.buildMarkdownLink).toBeDefined();
    });
    test.todo('buildMarkdownLink should work correctly');
    test('isValidName should be defined', () => {
        expect(index.isValidName).toBeDefined();
    });
    test.todo('isValidName should work correctly');
    test('canCreateFolder should be defined', () => {
        expect(index.canCreateFolder).toBeDefined();
    });
    test.todo('canCreateFolder should work correctly');
    test('createDirectory should be defined', () => {
        expect(index.createDirectory).toBeDefined();
    });
    test.todo('createDirectory should work correctly');
    test('isEmptyDirectory should be defined', () => {
        expect(index.isEmptyDirectory).toBeDefined();
    });
    test.todo('isEmptyDirectory should work correctly');
    test('cleanupEmptyDirectories should be defined', () => {
        expect(index.cleanupEmptyDirectories).toBeDefined();
    });
    test.todo('cleanupEmptyDirectories should work correctly');
    test('isDiplodocSection should be defined', () => {
        expect(index.isDiplodocSection).toBeDefined();
    });
    test.todo('isDiplodocSection should work correctly');
    test('isLanguageRoot should be defined', () => {
        expect(index.isLanguageRoot).toBeDefined();
    });
    test.todo('isLanguageRoot should work correctly');
    test('getLanguageRoot should be defined', () => {
        expect(index.getLanguageRoot).toBeDefined();
    });
    test.todo('getLanguageRoot should work correctly');
    test('decodeImagePath should be defined', () => {
        expect(index.decodeImagePath).toBeDefined();
    });
    test.todo('decodeImagePath should work correctly');
    test('normalizePathForKey should be defined', () => {
        expect(index.normalizePathForKey).toBeDefined();
    });
    test.todo('normalizePathForKey should work correctly');
    test('getRelativeLink should be defined', () => {
        expect(index.getRelativeLink).toBeDefined();
    });
    test.todo('getRelativeLink should work correctly');
    test('getRelativePath should be defined', () => {
        expect(index.getRelativePath).toBeDefined();
    });
    test.todo('getRelativePath should work correctly');
    test('slugify_diplodoc_reference should be defined', () => {
        expect(index.slugify_diplodoc_reference).toBeDefined();
    });
    test.todo('slugify_diplodoc_reference should work correctly');
    test('slugify_filename should be defined', () => {
        expect(index.slugify_filename).toBeDefined();
    });
    test.todo('slugify_filename should work correctly');
    test('slugify_latin should be defined', () => {
        expect(index.slugify_latin).toBeDefined();
    });
    test.todo('slugify_latin should work correctly');
    test('slugify_latin_alphanumeric should be defined', () => {
        expect(index.slugify_latin_alphanumeric).toBeDefined();
    });
    test.todo('slugify_latin_alphanumeric should work correctly');
    test('walk should be defined', () => {
        expect(index.walk).toBeDefined();
    });
    test.todo('walk should work correctly');
    test('IndexYamlEntryPatchHRef should be defined', () => {
        expect(index.IndexYamlEntryPatchHRef).toBeDefined();
    });
    test.todo('IndexYamlEntryPatchHRef should work correctly');
    test('IndexYamlEntryPatchSection should be defined', () => {
        expect(index.IndexYamlEntryPatchSection).toBeDefined();
    });
    test.todo('IndexYamlEntryPatchSection should work correctly');
    test('collectIndexMdFiles should be defined', () => {
        expect(index.collectIndexMdFiles).toBeDefined();
    });
    test.todo('collectIndexMdFiles should work correctly');
    test('getAllIndexMdFiles should be defined', () => {
        expect(index.getAllIndexMdFiles).toBeDefined();
    });
    test.todo('getAllIndexMdFiles should work correctly');
    test('TocYamlEntryCreate should be defined', () => {
        expect(index.TocYamlEntryCreate).toBeDefined();
    });
    test.todo('TocYamlEntryCreate should work correctly');
    test('TocYamlEntryUpdateOrAppend should be defined', () => {
        expect(index.TocYamlEntryUpdateOrAppend).toBeDefined();
    });
    test.todo('TocYamlEntryUpdateOrAppend should work correctly');
    test('TocYamlEntryRemove should be defined', () => {
        expect(index.TocYamlEntryRemove).toBeDefined();
    });
    test.todo('TocYamlEntryRemove should work correctly');
    test('TocYamlEntryPatch should be defined', () => {
        expect(index.TocYamlEntryPatch).toBeDefined();
    });
    test.todo('TocYamlEntryPatch should work correctly');
    test('TocYamlEntryPatchTitle should be defined', () => {
        expect(index.TocYamlEntryPatchTitle).toBeDefined();
    });
    test.todo('TocYamlEntryPatchTitle should work correctly');
    test('TocYamlEntryPatchReference should be defined', () => {
        expect(index.TocYamlEntryPatchReference).toBeDefined();
    });
    test.todo('TocYamlEntryPatchReference should work correctly');
    test('TocYamlEntryInsertAtPosition should be defined', () => {
        expect(index.TocYamlEntryInsertAtPosition).toBeDefined();
    });
    test.todo('TocYamlEntryInsertAtPosition should work correctly');
    test('TocYamlEntryMoveWithinSameFile should be defined', () => {
        expect(index.TocYamlEntryMoveWithinSameFile).toBeDefined();
    });
    test.todo('TocYamlEntryMoveWithinSameFile should work correctly');
    test('TocYamlFileLoad should be defined', () => {
        expect(index.TocYamlFileLoad).toBeDefined();
    });
    test.todo('TocYamlFileLoad should work correctly');
    test('TocYamlFileSave should be defined', () => {
        expect(index.TocYamlFileSave).toBeDefined();
    });
    test.todo('TocYamlFileSave should work correctly');
    test('sortTocItems should be defined', () => {
        expect(index.sortTocItems).toBeDefined();
    });
    test.todo('sortTocItems should work correctly');
    test('compareIndexes should be defined', () => {
        expect(index.compareIndexes).toBeDefined();
    });
    test.todo('compareIndexes should work correctly');
    test('indentedTocEntry should be defined', () => {
        expect(index.indentedTocEntry).toBeDefined();
    });
    test.todo('indentedTocEntry should work correctly');
    test('getTocIndentation should be defined', () => {
        expect(index.getTocIndentation).toBeDefined();
    });
    test.todo('getTocIndentation should work correctly');
    test('normalizeEmptyLines should be defined', () => {
        expect(index.normalizeEmptyLines).toBeDefined();
    });
    test.todo('normalizeEmptyLines should work correctly');
    test('getIndexFromBlock should be defined', () => {
        expect(index.getIndexFromBlock).toBeDefined();
    });
    test.todo('getIndexFromBlock should work correctly');
    test('splitTocIntoBlocks should be defined', () => {
        expect(index.splitTocIntoBlocks).toBeDefined();
    });
    test.todo('splitTocIntoBlocks should work correctly');
});
