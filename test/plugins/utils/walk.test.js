const walk = require('../../../src/plugins/utils/walk');

describe('walk', () => {
    test('walk should be defined', () => {
        expect(walk.walk).toBeDefined();
    });
    test.todo('walk should work correctly');
});
