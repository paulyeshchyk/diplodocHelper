const reindexer_figures = require('../../../src/plugins/reindexer/reindexer.figures');

describe('reindexer.figures', () => {
    test('reindexFigures should be defined', () => {
        expect(reindexer_figures.reindexFigures).toBeDefined();
    });
    test.todo('reindexFigures should work correctly');
});
