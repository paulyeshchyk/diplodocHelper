const diplodoc_helper_section_Rename = require('../../src/commands/diplodoc-helper.section.Rename');

describe('diplodoc-helper.section.Rename', () => {
    test('ux_section_rename should be defined', () => {
        expect(diplodoc_helper_section_Rename.ux_section_rename).toBeDefined();
    });
    test.todo('ux_section_rename should work correctly');
});
