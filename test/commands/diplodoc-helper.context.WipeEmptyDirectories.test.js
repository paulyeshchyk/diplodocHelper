const diplodoc_helper_context_WipeEmptyDirectories = require('../../src/commands/diplodoc-helper.context.WipeEmptyDirectories');

describe('diplodoc-helper.context.WipeEmptyDirectories', () => {
    test('ux_directories_wipe should be defined', () => {
        expect(diplodoc_helper_context_WipeEmptyDirectories.ux_directories_wipe).toBeDefined();
    });
    test.todo('ux_directories_wipe should work correctly');
});
