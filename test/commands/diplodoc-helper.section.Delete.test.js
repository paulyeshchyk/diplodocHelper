const diplodoc_helper_section_Delete = require('../../src/commands/diplodoc-helper.section.Delete');

describe('diplodoc-helper.section.Delete', () => {
    test('ux_section_delete should be defined', () => {
        expect(diplodoc_helper_section_Delete.ux_section_delete).toBeDefined();
    });
    test.todo('ux_section_delete should work correctly');
});
