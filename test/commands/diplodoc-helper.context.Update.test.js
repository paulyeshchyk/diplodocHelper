const diplodoc_helper_context_Update = require('../../src/commands/diplodoc-helper.context.Update');

describe('diplodoc-helper.context.Update', () => {
    test('ux_context_update should be defined', () => {
        expect(diplodoc_helper_context_Update.ux_context_update).toBeDefined();
    });
    test.todo('ux_context_update should work correctly');
});
