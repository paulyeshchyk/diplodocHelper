const diplodoc_helper_reindex_figures = require('../../src/commands/diplodoc-helper.reindex.figures');

describe('diplodoc-helper.reindex.figures', () => {
    test('ux_reindex_figures should be defined', () => {
        expect(diplodoc_helper_reindex_figures.ux_reindex_figures).toBeDefined();
    });
    test.todo('ux_reindex_figures should work correctly');
});
