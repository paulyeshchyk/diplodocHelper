const diplodoc_helper_section_Create = require('../../src/commands/diplodoc-helper.section.Create');

describe('diplodoc-helper.section.Create', () => {
    test('ux_section_create should be defined', () => {
        expect(diplodoc_helper_section_Create.ux_section_create).toBeDefined();
    });
    test.todo('ux_section_create should work correctly');
});
