const diplodoc_helper_reindex_directories = require('../../src/commands/diplodoc-helper.reindex.directories');

describe('diplodoc-helper.reindex.directories', () => {
    test('ux_reindex_directories should be defined', () => {
        expect(diplodoc_helper_reindex_directories.ux_reindex_directories).toBeDefined();
    });
    test.todo('ux_reindex_directories should work correctly');
});
