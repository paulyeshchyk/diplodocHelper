const diplodoc_helper_image_PasteFromList = require('../../src/commands/diplodoc-helper.image.PasteFromList');

describe('diplodoc-helper.image.PasteFromList', () => {
    test('ux_image_paste_list should be defined', () => {
        expect(diplodoc_helper_image_PasteFromList.ux_image_paste_list).toBeDefined();
    });
    test.todo('ux_image_paste_list should work correctly');
});
