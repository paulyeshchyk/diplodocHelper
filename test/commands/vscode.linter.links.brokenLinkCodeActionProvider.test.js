const vscode_linter_links_brokenLinkCodeActionProvider = require('../../src/commands/vscode.linter.links.brokenLinkCodeActionProvider');

describe('vscode.linter.links.brokenLinkCodeActionProvider', () => {
    test('BrokenLinkCodeActionProvider should be defined', () => {
        expect(vscode_linter_links_brokenLinkCodeActionProvider.BrokenLinkCodeActionProvider).toBeDefined();
    });
    test.todo('BrokenLinkCodeActionProvider should work correctly');
});
