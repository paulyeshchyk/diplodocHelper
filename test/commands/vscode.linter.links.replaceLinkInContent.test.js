const vscode_linter_links_replaceLinkInContent = require('../../src/commands/vscode.linter.links.replaceLinkInContent');

describe('vscode.linter.links.replaceLinkInContent', () => {
    test('replaceLinkInContent should be defined', () => {
        expect(vscode_linter_links_replaceLinkInContent.replaceLinkInContent).toBeDefined();
    });
    test.todo('replaceLinkInContent should work correctly');
});
