const diplodoc_helper_section_Move = require('../../src/commands/diplodoc-helper.section.Move');

describe('diplodoc-helper.section.Move', () => {
    test('ux_section_move should be defined', () => {
        expect(diplodoc_helper_section_Move.ux_section_move).toBeDefined();
    });
    test.todo('ux_section_move should work correctly');
});
