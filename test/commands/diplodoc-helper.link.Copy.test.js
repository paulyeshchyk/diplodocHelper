const diplodoc_helper_link_Copy = require('../../src/commands/diplodoc-helper.link.Copy');

describe('diplodoc-helper.link.Copy', () => {
    test('ux_link_copy should be defined', () => {
        expect(diplodoc_helper_link_Copy.ux_link_copy).toBeDefined();
    });
    test.todo('ux_link_copy should work correctly');
});
