const diplodoc_helper_link_AddAnchor = require('../../src/commands/diplodoc-helper.link.AddAnchor');

describe('diplodoc-helper.link.AddAnchor', () => {
    test('ux_add_anchor should be defined', () => {
        expect(diplodoc_helper_link_AddAnchor.ux_add_anchor).toBeDefined();
    });
    test.todo('ux_add_anchor should work correctly');
});
