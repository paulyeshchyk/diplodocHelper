const ContextMenuManager = require('../src/ContextMenuManager');

describe('ContextMenuManager', () => {
    test('ContextMenuManager should be defined', () => {
        expect(ContextMenuManager.ContextMenuManager).toBeDefined();
    });
    test.todo('ContextMenuManager should work correctly');
    test('PollingContextMenuManager should be defined', () => {
        expect(ContextMenuManager.PollingContextMenuManager).toBeDefined();
    });
    test.todo('PollingContextMenuManager should work correctly');
    test('ContextManager should be defined', () => {
        expect(ContextMenuManager.ContextManager).toBeDefined();
    });
    test.todo('ContextManager should work correctly');
});
